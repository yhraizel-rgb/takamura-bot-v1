services:
  takamura:
    build: .
    ports: ["8080:8080"]
    env_file: .env
    environment:
      RUNTIME_DIR: /app/runtime
    volumes:
      - takamura-runtime:/app/runtime
    restart: unless-stopped
    security_opt: ["no-new-privileges:true"]
volumes:
  takamura-runtime:
